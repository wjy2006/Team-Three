# 项目说明文档

## 1. 项目概述

- 项目类型：2D 叙事探索 + 战斗 + 道具/商店系统（Unity）
- 当前状态：开发中（未完成全部内容）
- 当前阶段目标：从起始区域推进流程，打开地图右上方的门。

本项目以场景切换为主线，结合对话、剧情触发、物品系统和战斗系统推进体验。玩家通过探索不同房间、与 NPC 互动、使用道具与武器，逐步解锁后续路径。

---

## 2. 项目结构（简要）

主要目录如下：

- `Assets/Game/Scenes`：游戏场景（Boot、各实验室房间、商店场景等）
- `Assets/Game/Scripts`：核心代码
- `Assets/Game/GameData`：数据资产（对话、剧情、物品、效果、本地化）
- `Assets/Game/Prefabs`：预制体（角色、子弹、火箭、门、出生点等）
- `ProjectSettings` / `Packages`：Unity 工程配置与依赖

脚本层按功能大致分为：

- `Core`：入口与常驻系统（`GameRoot`、`PauseManager`等等）
- `Systems`：剧情、对话、物品、场景流转、音频、本地化
- `Gameplay`：玩家控制、战斗、敌人、门、商店
- `UI`：HUD、淡入淡出等

---

## 3. 如何启动与游玩

## 3.1 启动方式

1. 使用 Unity `6000.3.7f1` 打开项目。
2. 打开并运行 `Assets/Game/Scenes/Boot.unity`。
3. 进入 Play 后会自动载入起始场景 `Room_Lab_PlayerHouse`。

## 3.2 当前版本游玩目标

- 打开地图右上方的门。

---

## 4. 操作说明（完整版）

## 4.1 角色移动

- `W/A/S/D` 或 `↑/←/↓/→`：移动

## 4.2 确认/交互

- `Z` / `Enter`：确认、交互

## 4.3 取消/防御（下蹲）

- `X` / `Shift` / `Esc`：取消
- 按住取消键：下蹲/防御姿态（用于部分战斗中的站定与抗击退）

## 4.4 菜单

- `C` / `Ctrl`：打开或关闭菜单
- 菜单内选择：`W/S` 或 `↑/↓`
- 菜单内左右切换（子选项）：`A/D` 或 `←/→`

## 4.5 对话

- 对话推进：`Z` / `Enter` / `Space` / 鼠标左键
- 对话取消/跳过当前打字显示：`X` / `Esc`（部分对话状态下）

## 4.6 手持物品与攻击

- 鼠标移动：瞄准方向
- 鼠标左键：使用当前手持物品

---

## 5. 作品特征

## 5.1 场景流转驱动的探索结构

玩家通过触发场景出口在多个房间间切换，形成“探索 -> 交互 -> 触发新房间”的推进节奏。

## 5.2 对话与剧情触发系统

- 对话支持重复对话与图结构对话（分支、条件、状态写入）。
- 剧情通过事件触发器驱动（如进入触发区、受击、使用手持物品等）。

## 5.3 物品与手持系统

- 背包与手持分离，支持拾取、交换、丢弃、使用。
- 武器与普通道具使用路径不同，体现战斗与资源管理差异。

## 5.4 战斗反馈

- 子弹命中、爆炸、击退、受击事件可参与剧情条件判断。
- 部分敌人（如炮台、闪避型 NPC）用于教学和机制展示。

## 5.5 视听与氛围

- 音乐为跨场景双通道淡入淡出切换。
- 支持中英文本地化表（CSV）。

---

## 6. 当前完成度说明

- 本项目处于进行中版本，不代表最终剧情与关卡规模。